# Recon Rover - navigation brain dependencies (Python 3, runs on the UNO Q Dragonwing).
# Pins are reasonable starting points for a hackathon; bump as needed.

# Vision and math
opencv-python==4.10.0.84
numpy==1.26.4

# Car link over USB serial
pyserial==3.5

# Web server + live map websocket to the phone
fastapi==0.115.0
uvicorn==0.30.6
websockets==13.0

# --- SLAM / perception backend ---
# DEFAULT: the phone (iPhone/iPad Pro camera + LiDAR + pose) via Record3D, which offloads
# SLAM onto the phone and streams RGBD + odometry over USB/WiFi. Uncomment to use it:
# record3d==1.4.1
#
# ALTERNATIVE (self-contained SLAM, if not using the phone): swap a backend behind
# slam/slam_frontend.py. Candidates: RTAB-Map (rtabmap), ORB-SLAM3, OpenVINS, Kimera.

# --- Object detection (YOLO): optional, for the "find a target" mission. ---
# perception/detector.py runs YOLO on the camera frames to find a target object. A tiny
# model (yolov8n) is enough for the edge. Uncomment to enable:
# ultralytics==8.3.0

# --- IMU / ToF driver: only needed if NOT using the phone. ---
# The phone's fused IMU makes the Modulino IMU/ToF redundant. If you keep them,
# bridge/modulino_io.py reads them over Qwiic / I2C. e.g. smbus2==0.4.3
